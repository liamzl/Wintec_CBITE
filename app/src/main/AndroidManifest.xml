<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chinenzl.wintec_cbite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Student_homePage" />
         <activity android:name=".Teacher_Login" />
        <activity android:name=".Teacher_homePage" />
        <activity
            android:name=".Student_courseList"
            android:label="@string/title_activity_student_course_list" />
        <activity
            android:name=".Teacher_semester"
            android:label="@string/title_activity_teacher_semester" />
        <activity android:name=".Teacher_courseList" />
        <activity android:name=".Teacher_pathCourseList" />
        <activity android:name=".Teacher_programmeDetail" />
        <activity android:name=".Teacher_inputStudent" />
        <activity android:name=".Teacher_successful" />
    </application>

</manifest>